apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ template "tkf-docker-calibre-web.name" . }}
  namespace: default
spec:
  secretName: {{ .Values.ingress.tls.secretName }}
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - {{ .Values.dnsName }}
