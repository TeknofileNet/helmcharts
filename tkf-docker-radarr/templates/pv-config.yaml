{{- if .Values.persistence.enabled }}
{{- if not .Values.persistence.existingClaim }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "tkf-docker-radarr.name" . }}-config
  labels:
    app: {{ template "tkf-docker-radarr.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  capacity:
    storage: {{ .Values.persistence.size | quote }}
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.nfs.enabled }}
  nfs:
    path: {{ .Values.persistence.nfs.configPath }}
    server: {{ .Values.persistence.nfs.configServer }}
  {{- end }}
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: default
    name: {{ template "tkf-docker-radarr.name" . }}-config
{{- end }}
{{- end }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "tkf-docker-radarr.name" . }}-media
  labels:
    app: {{ template "tkf-docker-radarr.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  capacity:
    storage: {{ .Values.persistence.size | quote }}
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.nfs.enabled }}
  nfs:
    path: {{ .Values.persistence.nfs.mediaPath }}
    server: {{ .Values.persistence.nfs.mediaServer }}
  {{- end }}
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: default
    name: {{ template "tkf-docker-radarr.name" . }}-media
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "tkf-docker-radarr.name" . }}-nzbget
  labels:
    app: {{ template "tkf-docker-radarr.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  capacity:
    storage: {{ .Values.persistence.size | quote }}
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.nfs.enabled }}
  nfs:
    path: {{ .Values.persistence.nfs.nzbgetPath }}
    server: {{ .Values.persistence.nfs.nzbgetServer }}
  {{- end }}
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: default
    name: {{ template "tkf-docker-radarr.name" . }}-nzbget
