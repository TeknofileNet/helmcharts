apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-swag.fullname" . }}
  labels:
    {{- include "tkf-docker-swag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-swag.selectorLabels" . | nindent 4 }}
