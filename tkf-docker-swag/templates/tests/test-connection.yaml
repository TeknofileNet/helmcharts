apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tkf-docker-swag.fullname" . }}-test-connection"
  labels:
    {{- include "tkf-docker-swag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tkf-docker-swag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
