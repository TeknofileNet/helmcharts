apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-gogs.fullname" . }}
  labels:
    {{- include "tkf-docker-gogs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-gogs.selectorLabels" . | nindent 4 }}
