apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-consul.fullname" . }}
  labels:
    {{- include "tkf-docker-consul.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-consul.selectorLabels" . | nindent 4 }}
