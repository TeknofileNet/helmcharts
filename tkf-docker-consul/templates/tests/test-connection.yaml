apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tkf-docker-consul.fullname" . }}-test-connection"
  labels:
    {{- include "tkf-docker-consul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tkf-docker-consul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
