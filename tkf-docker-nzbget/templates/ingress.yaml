apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tkf-docker-nzbget.fullname" . }}
spec:
  ingressClassName: nginx
  tls:
  - hosts:
      - nzbget.cosprings.teknofile.net
    secretName: tkf-global-wildcard-tls
  rules:
    - host: nzbget.cosprings.teknofile.net
      http:
        paths:
          - backend:
              service:
                name: {{ include "tkf-docker-nzbget.fullname" . }}
                port:
                  number: 6789 
            path: /
            pathType: Prefix
