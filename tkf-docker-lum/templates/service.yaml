apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-lum.fullname" . }}
  labels:
    {{- include "tkf-docker-lum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-lum.selectorLabels" . | nindent 4 }}
