apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-heimdall.fullname" . }}
  labels:
    {{- include "tkf-docker-heimdall.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-heimdall.selectorLabels" . | nindent 4 }}
