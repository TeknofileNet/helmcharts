apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-docker-smokeping.fullname" . }}
  labels:
    {{- include "tkf-docker-smokeping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-docker-smokeping.selectorLabels" . | nindent 4 }}
