apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tkf-docker-mariadb.fullname" . }}-test-connection"
  labels:
    {{- include "tkf-docker-mariadb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tkf-docker-mariadb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
