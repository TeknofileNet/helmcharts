apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tkf-smokeping.fullname" . }}-test-connection"
  labels:
    {{- include "tkf-smokeping.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tkf-smokeping.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
