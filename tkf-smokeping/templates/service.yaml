apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkf-smokeping.fullname" . }}
  labels:
    {{- include "tkf-smokeping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkf-smokeping.selectorLabels" . | nindent 4 }}
